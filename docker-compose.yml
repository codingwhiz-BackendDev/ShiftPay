version: '3.8'

services:
  web:
    build: .
    container_name: shiftpay
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
    restart: unless-stopped
